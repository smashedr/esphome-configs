# https://esphome.io/components/debug
platform: debug
device:
  name: "Device Info ${box_num}"
  id: debug_device_info
  disabled_by_default: true
## Packages does not merge this and reset_reason is included in common.yaml
#reset_reason:
#  name: "Reset Reason ${box_num}"
#  id: debug_reset_reason
#  disabled_by_default: true
