platform: template
name: "Update ${name} ${box_num}"
id: "${id}"
icon: mdi:update
disabled_by_default: true
on_press:
  - logger.log: "Update: ${name}: ${component}"
  - component.update: ${component}
